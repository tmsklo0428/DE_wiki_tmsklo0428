## 📅 2026-08-18(화)

### 📝 Review

- **DE 수업**: Spark의 AQE(Adaptive Query Execution)를 중심으로 실행 중 실제 데이터 통계를 활용한 파티션 최적화, Join 전략 변경, Skew Join 처리 방법을 학습했다.
또한 LocalShuffleReader와 Task Locality를 통해 Shuffle 과정의 네트워크 전송을 줄이고 실행 성능을 최적화하는 방법을 학습했다.
- **DE 프로젝트**
  - 공사 승인 데이터를 PostgreSQL에 별도 저장하고, Airflow DAG에서 기존 데이터와 병합해 Traffic Score를 재계산하는 구조를 설계했다.
  - 승인 담당자·시각을 기록하고, permit_id를 기준으로 승인 이력 추적 및 중복 반영을 방지하도록 설계했다.

### 🔄 KPT

**Keep**
- 기능 구현 전에 전체 데이터 흐름을 고민하면서 설계한 점을 유지한다.

**Problem**
- 처음에는 Open API로 수집하는 공사 데이터만 생각해, 실제 시스템에는 담당자가 승인한 공사를 관리하는 내부 DB가 별도로 존재할 수 있다는 점을 고려하지 못했다.
  → 다음에는 데이터 파이프라인을 설계할 때 외부 데이터뿐만 아니라 서비스 내부에서 생성·저장되는 데이터가 무엇인지 먼저 확인한다.
- 중복 승인과 같은 멱등성 문제를 설계 후반에 고려했다.
  → DB에 데이터를 저장할 때 같은 요청이 두 번 들어오면 어떻게 되는지 먼저 확인한다.
