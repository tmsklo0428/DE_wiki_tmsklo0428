# 2026-07-01

## Review
- **W1m1**: `mtcars` 데이터셋을 활용하여 다양한 DataFrame 메서드를 사용한 데이터 분석 과제를 수행했다
- **W1m3**: GDP 데이터를 추출(Extract)하고, Region 컬럼을 추가하는 등 데이터를 변환(Transform)한 후 JSON 파일로 저장(Load)하는 ETL 프로세스를 구현했다

## KPT

### Keep
- 새로운 함수나 메서드는 직접 실행해 보며 사용법과 결과를 확인하는 학습 방식을 계속 유지하기
- 그래프를 작성할 때 `plt.grid()` 등을 활용하여 가독성을 높이고, 데이터를 더 직관적으로 확인하려는 습관을 유지하기

### Problem
- 그래프를 처음 확인할 때 가독성이 떨어져 데이터를 해석하는 데 어려움이 있었다
- 상관계수를 해석할 때 어떤 값이 강한 상관관계를 의미하는지 명확하게 이해하지 못했다

### Try
- 그래프를 작성할 때 `plt.grid()`를 적극 활용하여 데이터 해석이 쉽도록 시각화를 개선하기
- 상관계수를 해석할 때는 부호뿐만 아니라 절댓값을 함께 확인하고, **1 또는 -1에 가까울수록 상관관계가 강하다**는 기준으로 분석하기
