## 📅 2026-07-27(월)

### 📝 Review

- **DE 수업**: RDD/DAG 기반 Spark 실행 구조(Transformation vs Action, Narrow/Wide Transformation, Job-Stage-Task, DAG Scheduler)와 MapReduce 대비 장점, RDD vs DataFrame 차이를 학습했다.
- **w5m1 과제**: PySpark RDD API로 NYC TLC 택시 데이터를 로드/클리닝하고, filter·map·reduce·groupBy 등 5개 이상의 transformation을 적용해 총 운행 건수, 총 매출, 평균 거리, 일별 건수/매출을 집계했다. 
- **최DE 프로젝트 아이디어 회의**: '고장'과 관련된 주제로 어떤 데이터 프로덕트를 만들면 좋을지 탐색했다.

### 🔄 KPT

**Keep**
- 문제를 해결한 뒤에도 "왜 이렇게 헷갈렸는지"를 단계별로 재구성해서 정리했다.

**Problem**
- `docker ps`나 컨테이너 로그만 보고 "컨테이너가 정상이니 문제없다"고 판단해서, 호스트 쪽 포트 충돌 가능성을 놓쳤다.
  다음부터는 접속이 안 될 때 `lsof -i :포트번호`로 호스트 포트 점유 여부를 먼저 체크하는 걸 습관화하자.
